import React from 'react';

export default class About extends React.Component {
  render() {
    return <h1>Unfortunately I was unable to figure out how to 
    remove the search engine when re routing to
    another page or add further content in each page.</h1>;
  }
}